cmake_minimum_required(VERSION 3.0) 

set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
set(CMAKE_BUILD_TYPE Release)

project(autofocus_v9)

find_package(OpenCV REQUIRED )
find_package(PkgConfig REQUIRED)

pkg_search_module(LIBUSB1 REQUIRED libusb-1.0)
include_directories(SYSTEM ${LIBUSB1_INCLUDE_DIRS})

#find_package(libusb REQUIRED)
#include_directories (${LIBUSB_1_INCLUDE_DIRS})
pkg_check_modules (GTK2 REQUIRED gtk+-2.0)

include_directories (${GTK2_INCLUDE_DIRS})
link_directories (${GTK2_LIBRARY_DIRS})

#pkg_check_modules(GTKMM gtk-2.0)
#pkg_check_modules(TOON TooN)
#pkg_check_modules(OPENCV OpenCV)

include_directories(${GTKMM_INCLUDE_DIRS})
link_directories (${GTKMM_LIBRARY_DIRS})
add_definitions (${GTKMM_CFLAGS_OTHER})

# include_directories(${TOON_INCLUDE_DIRS})
# link_directories (${TOON_LIBRARY_DIRS})
# add_definitions (${TOON_CFLAGS_OTHER})

include_directories(Vimba_6_0)

add_executable(autofocus_v9 HVI.cc autofocus.cc lens.cc tiltedcam.cc pid.cc)

#configure_file(${CMAKE_SOURCE_DIR}/Vimba_6_0/VimbaGigETL.cti ${CMAKE_BINARY_DIR}/VimbaGigETL.cti COPYONLY)
add_definitions (${GTK2_CFLAGS_OTHER})
#target_compile_options(hvi-gtk PRIVATE -Wall -Wextra)
#target_link_libraries (autofocus_v9 ${GTKMM_LIBRARIES} ${TOON_LIBRARIES} ${OpenCV_LIBS} cvd VimbaCPP ${CMAKE_SOURCE_DIR}/libASICamera2.a ${LIBUSB_1_LIBRARIES})
target_link_libraries (autofocus_v9 ${GTK2_LIBRARIES} ${TOON_LIBRARIES} ${OpenCV_LIBS} cvd VimbaCPP ${CMAKE_SOURCE_DIR}/libASICamera2.a -lusb-1.0)
